import { PortfolioPage } from "@/components/portfolio/PortfolioPage"

// General mode — ISB / networking / broad positioning. Same engine as
// /design-lead and /pm; only the content config differs (data/portfolio).
export default function Page() {
  return <PortfolioPage mode="general" />
}
